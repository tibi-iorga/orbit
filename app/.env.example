# Database (PostgreSQL connection string)
# For local development, use: postgresql://user:password@localhost:5432/orbit?schema=public
# For Vercel Postgres, this will be auto-provided when you add the integration
DATABASE_URL=

# NextAuth (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# OpenAI for auto-cluster and report summary (optional)
OPENAI_API_KEY=
