# Database (Supabase PostgreSQL)
# For local dev: run `npx vercel env pull .env.local` to pull from Vercel
# Or set manually from Supabase dashboard > Settings > Database
DATABASE_URL=
DIRECT_URL=

# NextAuth (generate with: openssl rand -base64 32)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000

# OpenAI for auto-cluster and report summary (optional)
OPENAI_API_KEY=
